/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["img.clerk.com"], // replace 'example.com' with the actual domain of user.imageUrl
  },
};

export default nextConfig;
